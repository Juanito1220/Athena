--tablas
--conjunto de datos ordenas por filas y columnas

--columnas
--donde se almacena un dato
	--int - entero 2mil millones
	--bigint 
	--smallint -- 32000 - 32000
	--tinyint 255
	--varchar(max) texto
	--char(5)
	--date fecha
	--datetime fecha con hora
	--bit  0 o 1

--filas son un conjunto de datos
	
--vistas
--instruccion sql encapsulada

--procedimientos almacenados
--codigo T SQL encapsulado

--funciones
--son objetos que pueden recibir o no parametros y devuelven un valor

--trigers
--desencadenadores que se ejecutan en base a un evento

--job
--trabajos
--son rutinas con intrucciones sql que pueden programarse

--indices
--un tipo de objeto asociado a una tabla que ayuda al gestor a enconetrar un dato